# Sprint 5 task FE-01 — UMI build-time env (DECISIONS A6).
# Copy → `.env` cho local dev. KHÔNG commit `.env`.

REACT_APP_ENV=dev
REACT_APP_API_URL=http://localhost:3002
REACT_APP_CDN_BASE=http://localhost:3002/uploads

# Optional — chỉ set khi cần override default trong config/config.ts.
# REACT_APP_ADMINISTRATIVE_API=
# REACT_APP_SHOW_BRANCH=1
